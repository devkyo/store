import { ThemeToggle } from "@/components/theme-toggle";
import { Button } from "@/components/ui/button";

const Page = () => {
    return (
        <div>
            <Button>Test</Button>
            <ThemeToggle />
        </div>
    );
};

export default Page;
